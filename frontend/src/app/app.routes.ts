import { Routes } from '@angular/router';
import { GetDatosComponent } from './components/get-datos/get-datos.component';

export const routes: Routes = [
    { path: '', component: GetDatosComponent}
];
